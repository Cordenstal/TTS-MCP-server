[project]
name = "tabletop-simulator-mcp"
version = "0.1.0"
description = "Local MCP server that lets Codex inspect and control Tabletop Simulator."
requires-python = ">=3.10"
dependencies = [
    "mcp>=1.0,<2",
    "mss>=10.0",
    "Pillow>=11.0",
]

[tool.uv]
package = false
